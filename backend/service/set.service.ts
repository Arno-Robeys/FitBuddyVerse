/*import setDb from "../domain/data-access/set.db";
import { ExerciseSet } from "../domain/model/set";


const createSet = async (sets: ExerciseSet): Promise<ExerciseSet> => {
    const createdSet = await setDb.createSet(sets);
    return createdSet;
}

export default {
    createSet
};*/
